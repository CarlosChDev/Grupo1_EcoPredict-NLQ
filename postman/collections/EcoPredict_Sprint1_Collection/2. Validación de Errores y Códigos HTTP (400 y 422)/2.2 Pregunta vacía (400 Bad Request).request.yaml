$kind: http-request
url: "{{base_url}}{{webhook_path}}"
method: POST
headers:
  Content-Type: application/json
  X-API-Key: "{{api_key}}"
body:
  type: text
  content: |-
    {
      "pregunta": "   "
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Validacion de pregunta vacia (400 o 200 controlado)', function () {
          pm.expect(pm.response.code).to.be.oneOf([200, 400, 422]);
      });
    language: text/javascript
order: 2000
